<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/repokun"
        android:label="@string/app_name"
        android:roundIcon="@drawable/repokun"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidEngineerCodeCheck"
        android:fullBackupContent="@xml/backup_descriptor"
        android:name=".GithubSearchApp">

        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="jp.co.yumemi.android.code_check.ui.TopActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>